Validator
=========

Validator component


Installation
============

Install with composer:

{
    "require": {
        "nautilus/validator": "dev-master"
    },
    "repositories": [
        {
            "type": "vcs",
            "url": "https://github.com/renq/Validator.git"
        }
    ]
}

